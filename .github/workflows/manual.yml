name: 🔁 Trigger Jenkins Build

on:
  push:
    branches:
      - main

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Trigger Jenkins Job
        run: |
          curl -X POST "https://jenkins.aakash1z.online:8080/job/internfy-client/build?token=deployfromgithub"
        env:
          JENKINS_URL: https://jenkins.aakash1z.online
          JENKINS_USER: aakash
          JENKINS_API_TOKEN: 11fd9fadefe223aa6a577e942da1cd7e52
